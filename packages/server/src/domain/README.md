# domain

Server-owned stable concepts, invariants, and domain rules.

Only place code here if the server package truly owns the concept or rule.
Do not use this as a generic bucket for orchestration or adapters.
