# app

Boot wiring and process composition root for `@mia/server`.

This folder is the target home for:

- server entrypoint
- boot-time setup
- environment/config loading
- package composition

Live code is still being migrated from older top-level files.