/**
 * Functional-core compatibility barrel for server application flow.
 *
 * Intentionally narrow at the start. Existing runtime-heavy orchestration still
 * lives under `orchestrator/` and is surfaced via `application/shell/` first.
 */

export {}