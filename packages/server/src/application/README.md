# application

Server-side use-case flow and orchestration.

This folder is the target home for:

- pure coordination logic in `core/`
- stateful coordinators and runners in `shell/`

This is run/use-case coordination, not durable storage and not transport edge.