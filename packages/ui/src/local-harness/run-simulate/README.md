# Local run-simulate harness (NOT a product feature)

Toolbar “Local sim” control for LLM-free paced SSE on your laptop.

Requires `VITE_LOCAL_RUN_SIMULATE=1` **and** Vite DEV, plus server `MIA_LOCAL_RUN_SIMULATE=1`.

See `packages/server/src/local-harness/run-simulate/README.md` for delete steps.
