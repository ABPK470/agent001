{
  "name": "@mia/sql-kit",
  "version": "0.1.0",
  "description": "Shared relational SQL helpers for Mia platform store, Sync warehouse dialects, and Bridge connectors. Quoting, literals, transient-error taxonomy — not a query builder or ORM.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.16.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
