{
  "name": "@agent001/engine",
  "version": "0.1.0",
  "description": "Agentic Automation Platform — declarative workflow engine where AI agents execute end-to-end work across UIs and APIs with governance built in.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.3.3",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/node": "^22.16.4",
    "prettier": "^3.8.1",
    "tsx": "^4.7.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
