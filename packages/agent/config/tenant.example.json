{
  "_comment": "Example tenant config (like .env.example). Copy → deploy/tenant.json. Guide: packages/agent/config/TENANT-CONFIG.md",

  "_comment_largeObjectRows": "Rows above this = large object for SQL validator (default 10M)",
  "largeObjectRows": 10000000,

  "_comment_unionBranchThreshold": "UNION branches before branch-aggregation doctrine (default 8)",
  "unionBranchThreshold": 8,

  "_comment_mirrorSchema": "Materialised-view mirror schema prefix. null = no mirror convention.",
  "mirrorSchema": "persistedview",

  "_comment_schemaRanking": "search_catalog ranking boost per schema",
  "schemaRanking": [
    { "schema": "publish", "weight": 50 },
    { "schema": "persistedview", "weight": 45 },
    { "schema": "fact", "weight": 20 },
    { "schema": "dim", "weight": 20 },
    { "schema": "list", "weight": 5 },
    { "schema": "archive", "weight": -20 },
    { "schema": "etl", "weight": -20 }
  ],

  "_comment_domainKeywords": "Words users type in goals — business vocabulary only. NOT sync entity ids (loaded from definitions.bundle.json at boot). NOT schema names (from live catalog).",
  "domainKeywords": [
    "client",
    "clients",
    "customer",
    "customers",
    "banker",
    "bankers",
    "revenue",
    "balance",
    "balances",
    "transaction",
    "transactions",
    "merchant",
    "merchants",
    "risk",
    "rwa",
    "impairment",
    "trading",
    "market",
    "markets",
    "sales credit",
    "sales credits",
    "africaflex",
    "africabrains",
    "FrontArena",
    "UnoTranspose",
    "IMEX",
    "country",
    "countries",
    "branch",
    "branches",
    "cost centre",
    "cost center",
    "counterparty",
    "facility",
    "book group",
    "segment",
    "breakdown",
    "exposure",
    "mymi",
    "uspSync"
  ],

  "_comment_preAggregationTokens": "Column suffixes where SUM is wrong for your warehouse model",
  "preAggregationTokens": [
    "Average",
    "Avg",
    "Mean",
    "Median",
    "Spot",
    "EOM",
    "Eod",
    "Latest",
    "Snapshot",
    "EndOf",
    "AsOf",
    "StartOf",
    "MTD",
    "YTD",
    "QTD",
    "WTD"
  ],

  "_comment_aliasFamilies": "SQL alias prefix → aggregate function",
  "aliasFamilies": [
    { "prefix": "Sum", "aggregate": "SUM" },
    { "prefix": "Total", "aggregate": "SUM" },
    { "prefix": "Avg", "aggregate": "AVG" },
    { "prefix": "Mean", "aggregate": "AVG" },
    { "prefix": "Min", "aggregate": "MIN" },
    { "prefix": "Max", "aggregate": "MAX" },
    { "prefix": "Count", "aggregate": "COUNT" },
    { "prefix": "NetRev", "aggregate": "SUM" },
    { "prefix": "GrossRev", "aggregate": "SUM" }
  ],

  "_comment_reservedAliases": "Disallowed SQL column aliases",
  "reservedAliases": ["publish", "fact", "dim", "core", "gate", "archive", "persistedview"],

  "_comment_catalogBootstrap": "OPTIONAL — tests/offline only. Omit in production when MSSQL catalog is available.",
  "catalogBootstrap": {
    "largeObjects": ["publish.revenue", "persistedview.publish.revenue"],
    "canonicalQualifiedNames": {
      "publish.revenue": "publish.Revenue",
      "persistedview.publish.revenue": "persistedView.publish.Revenue"
    },
    "unionBranchCounts": {
      "publish.revenue": 59
    },
    "highCardinalityKeys": {
      "publish.revenue": ["pkClient", "pkBranch", "pkAccount"]
    }
  }
}
