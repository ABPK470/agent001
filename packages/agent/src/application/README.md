# application

Agent application flow and coordination.

This is the target home for:

- pure coordination logic in `core/`
- stateful drivers and runtime coordinators in `shell/`

Use `domain/` for stable concepts and rules, not for application flow.