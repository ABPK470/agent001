# Shell command tool

**What:** Run OS commands with deny-rules.  
**Why:** Some tasks need a process (tests, builds). Named `shell-command` so it
is not confused with runtime “shell” (stateful drivers) or platform shell.  
**Next:** Bound to a `ShellClient` from the host.
